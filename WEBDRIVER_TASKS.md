# Webdriver Tasks — Repo only

> Scope trimmed to the repo. Everything else (credits ✅, Discord ✅, submission, slide, social) is handled by Dima, not webdriver.
> Fill `<GH_OWNER>` / `<REPO>` (e.g. `dinoraptor101` / `aida`).

## Fastest path (BUILDER, CLI) — create public from the start
```
gh repo create <REPO> --public --source=/Library/Repos/aida --remote=origin --push
```

## If it already exists PRIVATE — flip it (webdriver)
```
Go to https://github.com/<GH_OWNER>/<REPO>/settings
Scroll to the "Danger Zone" at the bottom.
Click "Change visibility" → "Make public" → confirm by typing the repo name → confirm.
Change NOTHING else.
```

## Verify (webdriver)
```
Signed out (or incognito), open https://github.com/<GH_OWNER>/<REPO>
Confirm the page loads. Report the public URL.
```

**Must be true before 5:00 PM submission: the repo is public.**

---

# Social posts (approved by Dima)

> Accessibility framing only. No personal/health details. Attach the **"Claude Build Day"** graphic. Confirm the signed-in account is Dima's; report the post URLs.

## LinkedIn (webdriver)
```
Open https://www.linkedin.com (signed in as Dima). Click "Start a post". Paste exactly:

I'm at the Live Claude Build Day with Anthropic and Cerebral Valley — a one-day
challenge to build something with Claude Code.

I'm building Aida: an accessibility tool that reads the emotional subtext in written
conversation — "captions for emotional subtext" — for neurodivergent people who find
tone and intent in text hard to read. It helps you understand what someone really
means, and catches a message before it lands the wrong way. Powered by Opus 4.8.

#ClaudeBuildDay

Attach the "Claude Build Day" graphic. Tag Anthropic and Cerebral Valley. Post. Report the URL.
```

## Facebook (webdriver)
```
Open https://www.facebook.com (signed in as Dima). Create a new post, audience = Public. Paste exactly:

Spending today at Anthropic & Cerebral Valley's Claude Build Day, building something I
care about: Aida — an accessibility tool that reads emotional subtext in text messages
for neurodivergent people. Think captions, but for the feeling underneath the words.
Built with Claude (Opus 4.8).

Attach the "Claude Build Day" graphic. Set to Public. Post. Report the URL.
```
